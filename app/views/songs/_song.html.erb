<%= turbo_frame_tag dom_id(song) do %>
 <div class="song">
  <div><%= song.title %></div>

  <div class="mt-2">
    <%= link_to "Edit", edit_song_path(song) %>
    <br/>
    <br/>
  </div>
</div>
<% end %> 
