<%= turbo_frame_tag dom_id(@song) do %>
  <%= render @song %>
<% end %>

<p>
  <strong>Title:</strong>
  <%= @song.title %>
</p>

<p>
  <strong>Url:</strong>
  <%= @song.url %>
</p>

<%= link_to 'Edit', edit_song_path(@song) %> |
<%= link_to 'Back', songs_path %>
